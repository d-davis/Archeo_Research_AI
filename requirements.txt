# ── Phase 1: Core ─────────────────────────────────────────────────────────────
ollama>=0.2.0
pandas>=2.0.0
openpyxl>=3.1.0
tiktoken>=0.6.0
rich>=13.7.0
requests>=2.31.0

# ── Phase 2: Geospatial ───────────────────────────────────────────────────────
# Install these via conda-forge (handles GDAL automatically):
#   conda install -c conda-forge geopandas rasterio pyproj shapely
# Or via pip if GDAL is already installed at system level:
geopandas>=0.14.0
pyproj>=3.6.0
shapely>=2.0.0
rasterio>=1.3.0
Pillow>=10.0.0

# ── Phase 2: CLIP semantic embeddings (optional) ──────────────────────────────
# Requires PyTorch first: https://pytorch.org/get-started/locally/
# Then: pip install open-clip-torch>=2.24.0

# ── Phase 3.6: PDF ────────────────────────────────────────────────────────────
pymupdf>=1.24.0
pymupdf4llm>=0.0.17

# ── Phase 4: GUI and charts ───────────────────────────────────────────────────
streamlit>=1.35.0
matplotlib>=3.8.0